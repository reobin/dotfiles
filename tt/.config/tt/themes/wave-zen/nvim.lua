-- kanagawa.nvim ships Wave on its own #1F1F28. The ground belongs to the theme
-- rather than to the plugin spec, so it travels as a global the spec reads back.
return {
  colorscheme = "kanagawa-wave",
  globals = {
    kanagawa_ground = "#090E13",
  },
}
