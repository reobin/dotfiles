#!/usr/bin/env bash

set -euo pipefail

if (($# == 0)); then
  echo "Usage: firefox-webapp <url> [firefox-args...]" >&2
  exit 1
fi

url="$1"
shift

exec setsid uwsm-app -- firefox --new-window "$url" "$@"
