# GTK apps: 2x scale everywhere
env = GDK_SCALE,2

# external monitor: fractional scale
monitor = DP-3,preferred,auto,1.6

# laptop panel; let hyprland pick the scale, mirror external when available
monitor = eDP-1,preferred,auto,auto
