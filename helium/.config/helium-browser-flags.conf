# Helium Browser Launch Flags
# https://github.com/imputnet/helium-linux
#
# This file is read by Helium's wrapper script on every launch.
# One flag or flag+value per line. Comments and blank lines are ignored.
#
# Custom extension loading (requires absolute path, no env vars):
--load-extension=/home/reobin/dotfiles/helium/extensions/alt-tab-switcher
#
# Examples:
# --enable-features=SidePanel
# --ozone-platform-hint=auto
# --password-store=kwallet5
