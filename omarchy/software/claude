#!/bin/sh

if pacman -Q claude-code >/dev/null 2>&1; then
  sudo pacman -Rns --noconfirm claude-code
fi

if ! command -v claude >/dev/null 2>&1; then
  curl -fsSL https://claude.ai/install.sh | bash
fi
